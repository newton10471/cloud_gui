<%= form_for(@instance) do |f| %>
  <% if @instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instance.errors.count, "error") %> prohibited this instance from being saved:</h2>

      <ul>
      <% @instance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :usercomment %><br />
    <%= f.text_field :usercomment %>
  </div>
  <div class="field">
    <%= f.label :baseimage %><br />
    <%= f.select :baseimage, Instance::IMAGES %>
  </div>
  <div class="field">
    <%= f.label :cpus %><br />
    <%= f.select :cpus, Instance::CPU_SIZES %>
  </div>
  <div class="field">
    <%= f.label :memory %><br />
    <%= f.select :memory, Instance::MEMORY_SIZES %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
